import React from 'react';
import { Box, Typography } from '@mui/material';

const KnowledgeSourceUpload: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Upload Knowledge Source</Typography>
      <Typography>File upload component will be here.</Typography>
    </Box>
  );
};

export default KnowledgeSourceUpload;
